{{- if .Values.streamingService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: streaming-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: streaming-service
spec:
  replicas: {{ .Values.streamingService.replicaCount }}
  selector:
    matchLabels:
      app: streaming-service
  template:
    metadata:
      labels:
        app: streaming-service
    spec:
      containers:
        - name: streaming-service
          image: "{{ .Values.streamingService.image.repository }}:{{ .Values.streamingService.image.tag }}"
          imagePullPolicy: {{ .Values.streamingService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.streamingService.service.port }}
          envFrom:
            - configMapRef:
                name: durga-streaming-config
            - secretRef:
                name: app-secrets
          resources:
            {{- toYaml .Values.streamingService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.streamingService.service.type }}
  selector:
    app: streaming-service
  ports:
    - port: {{ .Values.streamingService.service.port }}
      targetPort: {{ .Values.streamingService.service.port }}
{{- end }}
